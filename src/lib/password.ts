/** Company-wide password policy: at least 6 characters. */
export const MIN_PASSWORD_LENGTH = 6;

export const PASSWORD_HINT = `Minimum ${MIN_PASSWORD_LENGTH} characters`;

export const PASSWORD_TOO_SHORT = `Password must be at least ${MIN_PASSWORD_LENGTH} characters`;
